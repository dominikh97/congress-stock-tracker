name: Process Trade Alert Subscriptions

on:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write

jobs:
  process-subscription:
    runs-on: ubuntu-latest
    if: |
      startsWith(github.event.issue.title, 'Subscribe request:') ||
      startsWith(github.event.issue.title, 'Confirm subscription:') ||
      startsWith(github.event.issue.title, 'Unsubscribe request:')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Process subscription
        id: process
        env:
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          python process_subscription.py

      - name: Commit updated subscription state
        if: steps.process.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add alerts.json pending_subscriptions.json
          git commit -m "Update alert subscriptions from issue #${{ github.event.issue.number }}"
          git push

      - name: Comment on issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          COMMENT_BODY: ${{ steps.process.outputs.message }}
        run: |
          gh issue comment "$ISSUE_NUMBER" --body "$COMMENT_BODY"

      - name: Close issue
        if: steps.process.outputs.status == 'done'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          gh issue close "$ISSUE_NUMBER"
